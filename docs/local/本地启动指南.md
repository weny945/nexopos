# NexoPOS 本地开发环境启动指南

## 📋 目录

- [环境要求](#环境要求)
- [快速启动](#快速启动)
- [详细配置步骤](#详细配置步骤)
- [常用命令](#常用命令)
- [故障排除](#故障排除)
- [开发工作流](#开发工作流)

## 环境要求

- **PHP**: 8.2+
- **Composer**: 2.x
- **Node.js**: 18+ (推荐 20+)
- **NPM**: 9+
- **数据库**: MySQL 8.0+ 或 SQLite 3.x
- **Web 服务器**: Apache/Nginx（开发环境可用 `php artisan serve`）

## 快速启动

### 最小启动（2 个终端窗口）

**终端 1 - 后端服务器：**
```bash
php artisan serve
```

**终端 2 - 前端开发服务器：**
```bash
npm run dev
```

**浏览器访问：**
```
http://127.0.0.1:8000
```

### 完整启动（4 个终端窗口）

如果需要使用队列和 WebSocket 功能：

**终端 1 - Laravel 后端：**
```bash
php artisan serve
```

**终端 2 - Vite 前端（热更新）：**
```bash
npm run dev
```

**终端 3 - 队列处理器：**
```bash
php artisan queue:work
```

**终端 4 - WebSocket 服务器：**
```bash
php artisan reverb:start

```

## 详细配置步骤

### 1. 克隆项目（如果是新项目）

```bash
git clone <repository-url>
cd NexoPOS-master
```

### 2. 安装依赖

```bash
# 安装 PHP 依赖
composer install

# 安装前端依赖
npm install
```

### 3. 环境配置

```bash
# 复制环境配置文件
cp .env.example .env

# 生成应用密钥
php artisan key:generate
```

### 4. 数据库配置

编辑 `.env` 文件，配置数据库：

**选项 A - 使用 SQLite（推荐开发环境）：**
```env
DB_CONNECTION=sqlite
DB_DATABASE=D:/develop/PHP_project/NexoPOS-master/database/database.sqlite
```

创建 SQLite 数据库文件：
```bash
touch database/database.sqlite
```

**选项 B - 使用 MySQL：**
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=nexopos
DB_USERNAME=root
DB_PASSWORD=
DB_PREFIX=ns_
```

### 5. 应用 URL 配置

在 `.env` 中设置：
```env
APP_URL="http://127.0.0.1:8000"
SANCTUM_STATEFUL_DOMAINS="127.0.0.1:8000"
SESSION_DOMAIN="127.0.0.1"
```

### 6. 运行初始化命令

```bash
# 创建存储链接
php artisan storage:link

# 设置翻译文件链接
php artisan ns:translate --symlink --force

# 创建模块符号链接
php artisan modules:symlink

# 运行数据库迁移
php artisan migrate
```

### 7. 创建管理员账户

**方法 1 - 使用安装向导：**
- 启动服务器后，浏览器访问 `http://127.0.0.1:8000`
- 系统会自动跳转到安装向导

**方法 2 - 使用命令行：**
```bash
php artisan ns:create-user
```

按提示输入：
- 用户名
- 邮箱
- 密码
- 确认密码

### 8. 启动开发服务器

参考[快速启动](#快速启动)章节。

## 常用命令

### 开发命令

```bash
# 启动 Laravel 开发服务器（默认 8000 端口）
php artisan serve

# 指定端口启动
php artisan serve --port=8080

# 启动 Vite 前端开发服务器（热更新）
npm run dev

# 启动 Vite（监视模式）
npm run watch

# 构建生产环境前端资源
npm run build
```

### 数据库命令

```bash
# 运行迁移
php artisan migrate

# 回滚上一次迁移
php artisan migrate:rollback

# 重置数据库并重新运行所有迁移
php artisan migrate:fresh

# 运行迁移并填充数据
php artisan migrate --seed

# 查看迁移状态
php artisan migrate:status

# 创建数据库快照
php artisan snapshot:create my-snapshot

# 加载数据库快照
php artisan snapshot:load my-snapshot
```

### 缓存命令

```bash
# 清除所有缓存
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 优化缓存（生产环境）
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

### 模块和 CRUD 命令

```bash
# 创建新模块
php artisan module:make <ModuleName>

# 删除模块
php artisan module:delete <ModuleName>

# 创建模块符号链接
php artisan modules:symlink

# 生成 CRUD 资源
php artisan crud:make <CrudName>
```

### 翻译命令

```bash
# 设置翻译文件符号链接
php artisan ns:translate --symlink --force
```

### 测试命令

```bash
# 运行所有测试
./vendor/bin/phpunit

# 并行运行测试
./vendor/bin/phpunit --parallel

# 运行特定测试套件
./vendor/bin/phpunit --testsuite Sequential-1
./vendor/bin/phpunit --testsuite Parallel-1

# 运行特定测试
./vendor/bin/phpunit --filter CreateOrderTest

# 运行前端测试
npm test
```

### 代码质量命令

```bash
# Laravel Pint 代码格式化
./vendor/bin/pint

# 检查代码风格（不修复）
./vendor/bin/pint --test
```

### 队列和任务命令

```bash
# 启动队列处理器
php artisan queue:work

# 处理队列（只执行一次）
php artisan queue:work --once

# 查看失败的任务
php artisan queue:failed

# 重试失败的任务
php artisan queue:retry all
```

### WebSocket 命令

```bash
# 启动 Laravel Reverb WebSocket 服务器
php artisan reverb:start

# 在后台启动
php artisan reverb:start &
```

## 故障排除

### 1. 端口被占用

**问题：** 端口 8000 已被占用

**解决方法：**
```bash
# 使用其他端口
php artisan serve --port=8080

# 或者找到占用进程并终止（Windows）
netstat -ano | findstr :8000
taskkill /PID <进程ID> /F

# Linux/Mac
lsof -ti:8000 | xargs kill -9
```

### 2. 数据库连接失败

**问题：** `SQLSTATE[HY000] [2002] Connection refused`

**解决方法：**
- 检查 `.env` 中的数据库配置
- 确保数据库服务正在运行
- SQLite：确保数据库文件路径正确且文件存在
- MySQL：确保 MySQL 服务已启动

### 3. 权限错误（Linux/Mac）

**问题：** `Permission denied` 错误

**解决方法：**
```bash
chmod -R 775 storage bootstrap/cache
chown -R $USER:www-data storage bootstrap/cache
```

### 4. 前端资源 404

**问题：** 前端样式和脚本无法加载

**解决方法：**
```bash
# 清除缓存
php artisan cache:clear

# 重新构建前端资源
npm run build

# 确保 Vite 开发服务器正在运行
npm run dev
```

### 5. CSRF Token Mismatch

**问题：** `CSRF token mismatch` 错误

**解决方法：**
```bash
# 清除配置缓存
php artisan config:clear

# 确保 .env 中的 APP_URL 正确
# APP_URL="http://127.0.0.1:8000"
# SESSION_DOMAIN="127.0.0.1"

# 清除浏览器 Cookie
```

### 6. 模块资源 404

**问题：** 模块资源无法加载

**解决方法：**
```bash
php artisan modules:symlink
```

### 7. 无限加载

**问题：** 页面一直显示加载状态

**解决方法：**
1. 确保 `npm run dev` 正在运行
2. 检查浏览器控制台的错误信息
3. 重启开发服务器
4. 清除浏览器缓存

### 8. Composer 内存不足

**问题：** `Allowed memory size exhausted`

**解决方法：**
```bash
php -d memory_limit=-1 composer install
```

### 9. NPM 安装失败

**问题：** NPM 包安装失败

**解决方法：**
```bash
# 清除 NPM 缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install

# 或使用 Yarn
yarn install
```

## 开发工作流

### 日常开发流程

1. **启动开发环境**
   ```bash
   # 终端 1
   php artisan serve

   # 终端 2
   npm run dev
   ```

2. **进行代码修改**
   - 后端：修改 `app/`、`routes/`、`config/` 等目录
   - 前端：修改 `resources/ts/`、`resources/views/` 等目录

3. **前端改动自动刷新**
   - Vite 提供热模块替换（HMR）
   - 保存文件后浏览器自动更新

4. **后端改动需手动刷新**
   - 修改控制器、模型、路由后刷新浏览器
   - 修改配置文件后需要运行 `php artisan config:clear`

5. **运行测试**
   ```bash
   ./vendor/bin/phpunit
   ```

6. **代码格式化**
   ```bash
   ./vendor/bin/pint
   ```

### 添加新功能

1. **创建迁移**
   ```bash
   php artisan make:migration create_xxx_table
   php artisan migrate
   ```

2. **创建模型**
   ```bash
   php artisan make:model ModelName
   ```

3. **创建控制器**
   ```bash
   php artisan make:controller ControllerName
   ```

4. **创建 CRUD（NexoPOS 特有）**
   ```bash
   php artisan crud:make CrudName
   ```

5. **创建模块（NexoPOS 特有）**
   ```bash
   php artisan module:make ModuleName
   ```

### Git 工作流

```bash
# 拉取最新代码
git pull origin main

# 创建功能分支
git checkout -b feature/my-feature

# 提交更改
git add .
git commit -m "Add new feature"

# 推送分支
git push origin feature/my-feature
```

### 部署前检查

```bash
# 运行测试
./vendor/bin/phpunit

# 代码格式化
./vendor/bin/pint

# 构建生产资源
npm run build

# 清除开发缓存
php artisan cache:clear
php artisan config:clear

# 优化生产缓存
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

## 开发技巧

### 1. 使用配置文件快速切换环境

创建多个环境配置文件：
- `.env.local` - 本地开发
- `.env.testing` - 测试环境
- `.env.production` - 生产环境

切换环境：
```bash
cp .env.local .env
php artisan config:clear
```

### 2. 使用数据库快照加速测试

```bash
# 创建干净的数据库快照
php artisan migrate:fresh
php artisan snapshot:create clean

# 测试后快速恢复
php artisan snapshot:load clean
```

### 3. 查看实时日志

```bash
tail -f storage/logs/laravel.log
```

### 4. 使用 Tinker 调试

```bash
php artisan tinker

# 在 Tinker 中测试代码
>>> $user = App\Models\User::first()
>>> $user->name
```

### 5. 监控队列任务

```bash
# 启动队列并显示详细输出
php artisan queue:work --verbose
```

## Windows 环境特别说明

### Cygwin 环境

如果你使用 Cygwin，需要注意：

1. **路径格式**
   ```bash
   # Cygwin 路径：/cygdrive/d/develop/...
   # Windows 路径：D:/develop/...
   # .env 中使用 Windows 路径格式
   ```

2. **端口冲突**
   ```bash
   # Windows 防火墙可能阻止端口访问
   # 需要添加防火墙规则允许 8000 端口
   ```

3. **符号链接**
   ```bash
   # Windows 需要管理员权限创建符号链接
   # 或在 .env 中设置：
   FILESYSTEM_DRIVER=local
   ```

### PowerShell / CMD

使用 Windows 原生终端：

```powershell
# 启动后端
php artisan serve

# 启动前端（新终端）
npm run dev
```

## 一键启动脚本

### Linux/Mac 启动脚本

创建 `start.sh`：

```bash
#!/bin/bash

echo "🚀 启动 NexoPOS 开发环境..."

# 启动 Laravel 服务器（后台运行）
echo "📦 启动 Laravel 服务器..."
php artisan serve &
LARAVEL_PID=$!

# 启动 Vite 开发服务器
echo "⚡ 启动 Vite 开发服务器..."
npm run dev

# 捕获 Ctrl+C，清理进程
trap "kill $LARAVEL_PID; exit" INT
wait
```

使用：
```bash
chmod +x start.sh
./start.sh
```

### Windows 启动脚本

创建 `start.bat`：

```batch
@echo off
echo 启动 NexoPOS 开发环境...

echo 启动 Laravel 服务器...
start cmd /k "php artisan serve"

echo 启动 Vite 开发服务器...
start cmd /k "npm run dev"

echo.
echo ✅ 开发环境已启动！
echo 📱 访问: http://127.0.0.1:8000
pause
```

双击运行 `start.bat`

### PowerShell 启动脚本

创建 `start.ps1`：

```powershell
Write-Host "🚀 启动 NexoPOS 开发环境..." -ForegroundColor Green

Write-Host "📦 启动 Laravel 服务器..." -ForegroundColor Cyan
Start-Process powershell -ArgumentList "-NoExit", "-Command", "php artisan serve"

Write-Host "⚡ 启动 Vite 开发服务器..." -ForegroundColor Cyan
Start-Process powershell -ArgumentList "-NoExit", "-Command", "npm run dev"

Write-Host "`n✅ 开发环境已启动！" -ForegroundColor Green
Write-Host "📱 访问: http://127.0.0.1:8000" -ForegroundColor Yellow
```

使用：
```powershell
.\start.ps1
```

## 相关文档

- [项目概述 (CLAUDE.md)](../../CLAUDE.md)
- [库存管理系统完整指南](库存管理系统完整指南.md)
- [条形码扫描器快速上手指南](条形码扫描器快速上手指南.md)
- [官方文档](https://my.nexopos.com/en/documentation)
- [API 文档](https://docs.api.nexopos.com)

## 获取帮助

- GitHub Issues: https://github.com/Blair2004/NexoPOS/issues
- WhatsApp 社区: https://chat.whatsapp.com/KHWgNmfcfJy7SwJiRQTmG8
- 视频教程: https://www.youtube.com/watch?v=V80-hOJCywY

---

**最后更新**: 2026-01-23
**适用版本**: NexoPOS 5.x (Laravel 12.x)
**维护者**: NexoPOS 开发团队
